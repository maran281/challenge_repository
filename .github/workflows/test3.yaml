name: Merge test3 to test4

on:
  push:
    branches:
      - test3
      - test4

jobs:
  merge-branches:
    runs-on: ubuntu-latest

    steps:
      - name: Checking the current branch
        id: current-branch
        run: |

      - name: Checkout code
        if: github.event.ref == 'refs/heads/test3'
        run: echo "Workflow is running inside test3 branch"

      - name: Checkout code
        if: github.event.ref == 'refs/heads/test4'
        run: echo "Workflow is running inside test4 branch"